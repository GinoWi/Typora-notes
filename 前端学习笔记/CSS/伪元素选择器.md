### 伪元素选择器

什么是伪元素选择器？

伪元素选择器的作用就是给指定标签的内容前面添加子元素，或者给指定标签的内容后面添加子元素。无法设置宽度和高度，需要修改该元素的显示模式来变化该元素的宽度和高度。

格式：

```html
<!--给指定标签内容前面添加子元素-->
标签名称::before{
	content: ""
	属性
}

<!--给指定标签内容后面添加子元素-->
标签名称::after {
	content: ""
	属性
}
```

指定添加的子元素的高度为0并不影响显示内容，可以通过visibility:hidden;属性隐藏子元素